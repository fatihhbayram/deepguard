# PostgreSQL
POSTGRES_USER=deepguard
POSTGRES_PASSWORD=deepguard
POSTGRES_DB=deepguard

# MinIO
MINIO_ROOT_USER=deepguard
MINIO_ROOT_PASSWORD=deepguard123
MINIO_API_PORT=9002
MINIO_CONSOLE_PORT=9003

# Web
WEB_PORT=3000
# API base URL used by the web app (server-side fetch).
# Host dev:  http://localhost:8000
# Container: http://host.docker.internal:8000
NEXT_PUBLIC_API_URL=http://host.docker.internal:8000

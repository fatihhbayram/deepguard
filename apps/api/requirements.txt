# Speaker diarization runs a local PyTorch model, and PyPI's default torch wheels for Linux
# bundle the CUDA runtime — roughly two gigabytes of it. Diarization is short, CPU-bound work
# and this machine's single GPU is already spoken for, so the CPU-only index is used instead.
# The `+cpu` local versions sort above the plain PyPI ones, so these three resolve here while
# everything else still comes from PyPI.
--extra-index-url https://download.pytorch.org/whl/cpu

alembic==1.19.1
# Hashes account passwords with Argon2id (R1-T1). Named here as a direct dependency rather
# than relied on as somebody else's transitive one: this is now load-bearing security code,
# and a pin is what keeps an upgrade of an unrelated package from changing how passwords
# are hashed. The library's default parameters track RFC 9106 and are used as they come —
# see `app/web_auth.py`.
argon2-cffi==25.1.0
c2pa-python==0.37.7
fastapi==0.141.1
grpcio==1.76.0
minio==7.2.20
# Runs the AASIST audio anti-spoofing checkpoint (P6-T2). The CPU-only wheel — there is no
# GPU build here and none is wanted — is 17.4 MB down / ~52 MB installed, against a 1.6 MB
# checkpoint and ~60 MiB resident at inference. Pinned to the version P6-T1 benchmarked on,
# so the numbers in that report describe the same runtime that ships.
onnxruntime==1.23.2
# Decodes the frames the face-manipulation classifier is scored on, and runs the YuNet locator
# that finds the face in each (R3-T2). The headless wheel: this process has no display and the
# GUI build pulls in an X11 stack it would never call. Pinned to the version R3-T1 benchmarked
# the detector on, so the numbers in that report describe the same preprocessing that ships.
opencv-python-headless==5.0.0.93
protobuf==6.33.6
psycopg2-binary==2.9.12
pyannote.audio==4.0.7
python-dotenv==1.2.3
python-multipart==0.0.32
SQLAlchemy==2.0.52
torch==2.13.0+cpu
torchaudio==2.11.0+cpu
torchcodec==0.16.0+cpu
uvicorn==0.52.3
# Downloads media from public URLs (P10-T1). Used as a library, never as a subprocess: the
# URL is a Python argument rather than a token in a command line, so there is no shell for a
# crafted URL to reach. Pinned like everything else here, and worth re-pinning deliberately —
# extractors track sites that change under them, so an old pin quietly stops working while a
# blind upgrade changes behaviour nothing tested.
yt-dlp==2026.8.19
# What `face_alignment` needs and does not bring with it (R5-T2). The package itself is
# installed by the Dockerfile with `--no-deps`, so its own dependency list is not consulted,
# and these are the two entries from that list this image genuinely needs: `numba`, which its
# `utils` imports unconditionally, and `scikit-image`, the similarity warp that puts each frame
# on the LRW mean face. Both are pinned to the versions R5-T1 benchmarked the detector under.
#
# The two entries deliberately left behind are why the install is `--no-deps` at all.
# `opencv-python` would put a second, GUI-linked OpenCV beside the headless wheel above — two
# copies of one library in one process, one of them dragging in an X11 stack this container
# has no display for — and `dlib` is declared for a face detector this codebase never selects.
# `scipy` and `tqdm` are already here through pyannote.
numba==0.67.0
scikit-image==0.25.2
